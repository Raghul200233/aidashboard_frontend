# Local dev port
PORT=3002

# Backend URL — for local dev (points to local server)
# For Vercel production, set this as an Environment Variable in the Vercel dashboard:
#   REACT_APP_API_URL = https://your-backend-name.onrender.com
REACT_APP_API_URL=http://localhost:5002
